[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "clocky-sdk"
version = "0.0.0.dev0"
description = "Python SDK for Clocky"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Clocky" }]
dependencies = [
  "pydantic>=2.12,<3",
  "clocky-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/Linorman/clocky"
Documentation = "https://github.com/Linorman/clocky/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/Linorman/clocky/issues"
Source = "https://github.com/Linorman/clocky"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/clocky"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
clocky-runtime-bin = { path = "../sdk-runtime", editable = true }
