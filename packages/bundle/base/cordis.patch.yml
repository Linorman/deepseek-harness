# The clocky-base bundle patch: the shared core of every clocky profile, applied as
# ONE insert over the empty profile root. Later bundle patches and the user's
# profile cordis.patch.yml address these rows by id, with the last write
# winning per row.
#
# A patch replaces the targeted row's whole `config` rather than merging into
# it, so a row whose value differs by mode does NOT live here: it belongs to
# each mode bundle, keeping any single row down to one bundle layer plus the
# user's. Mode-specific rows appear below only with shared plugin identity and
# neutral defaults; each mode bundle restates its complete configuration.
#
# Row order carries no load semantics (activation is service-availability
# driven); the grouping is for readers.

- insert:
    - id: timer
      name: '@clocky/cordis-plugin-timer'

    - id: hmr
      name: '@clocky/cordis-plugin-hmr'
      config:
        root: ['.']

    - id: llm
      name: '@clocky/clocky-llm'

    - id: session
      name: '@clocky/clocky-session'

    - id: typert
      name: '@clocky/clocky-typert-registry'

    - id: typert-loader
      name: '@clocky/clocky-typert-loader'

    - id: typert-gateway
      name: '@clocky/clocky-api-gateway'

    - id: session-title
      name: '@clocky/clocky-session-title'
      config:
        fallbackMaxWords: 5
        fallbackMaxBytes: 40
        maxTitleBytes: 80

    - id: session-title-llm
      name: '@clocky/clocky-session-title-first-prompt-llm'
      config:
        targetWords: 5
        targetCjkCharacters: 10
        maxInputBytes: 4096
        maxOutputTokens: 64
        timeoutMs: 60000

    - id: user-questions
      name: '@clocky/clocky-user-questions'

    - id: agent
      name: '@clocky/clocky-agent'

    # The transport-independent default for Agents created by entry points.
    # Settings may supply a saved selection; an empty composition leaves the
    # provider/model unset until a deployment selects one.
    - id: agent-default-model
      name: '@clocky/clocky-agent-default-model'

    - id: jobs
      name: '@clocky/clocky-jobs-local'

    - id: llm-retry
      name: '@clocky/clocky-llm-retry'

    # User-settings document (`$CLOCKY_HOME/settings.yaml`, hot-reloaded): an
    # `llm-pi-ai:` section there supplies adapter routes
    # below without a restart, and is what the web Models page writes.
    - id: settings
      name: '@clocky/clocky-settings-file'

    # Credential sources: inherited environment over the managed
    # `$CLOCKY_HOME/.credentials.yaml`, with project and user `.env` fallbacks.
    # Adapters resolve references per request; the Models page writes only the
    # managed document, which is never materialized into the process environment.
    - id: credentials
      name: '@clocky/clocky-credentials-local'

    # The pi-ai multi-provider adapter, mounted dormant: zero routes (and no extra
    # models in the picker) until a `llm-pi-ai:` settings section supplies provider
    # profiles — then those routes register live, keys resolving per request
    # through their apiKeyEnv references, and drop again when the section empties.
    # Supplying those profiles is exactly what the web Models page does. Which
    # adapters exist is composition; which providers run is the user's settings
    # document.
    - id: llm-pi-ai
      name: '@clocky/clocky-llm-pi-ai'

    - id: session-persistence-jsonl
      name: '@clocky/clocky-session-persistence-jsonl'
      config:
        root: !!js clockyHomePath('sessions')

    # Durable image bytes live outside the append-only session log. Messages
    # keep content-addressed references that this shared backend resolves for
    # provider requests and authorized history reads.
    - id: attachment-local
      name: '@clocky/clocky-attachment-local'

    # Full-text session search is opt-in. `openAt: never` keeps
    # ctx.sessionQuery mounted — exact reads, titles, and lineage traces
    # (session export, subagent-fork Workspace inheritance) stay available —
    # while search calls fail with SESSION_QUERY_SEARCH_DISABLED and SQLite is
    # never opened; the Web sidebar search matches titles and workspace names
    # only. Deployments enabling content search override `openAt` to
    # `first-search` or `startup` in a later patch layer (profile
    # cordis.patch.yml or a --patch overlay), typically with a durable `path`.
    - id: session-query-sqlite
      name: '@clocky/clocky-session-query-sqlite'
      config:
        path: ':memory:'
        openAt: never

    # Shared projection registry: subagent catalog identity (mode/label) folds
    # through its registered units, so the `list_agents` surface below fails
    # loud without it; web layers reuse this same mount for list rows.
    - id: session-projection
      name: '@clocky/clocky-session-projection'

    - id: subprocess
      name: '@clocky/clocky-subprocess-local'

    # Every shipped CLI mode starts with the same file-effect boundary.
    # The environment remains an explicit deployment override; otherwise fresh
    # sessions pin workspace-write + ask through the permission service below.
    - id: sandbox
      name: '@clocky/clocky-sandbox-local'

    - id: sandbox-policy
      name: '@clocky/clocky-sandbox-policy'
      config:
        mode: !!js process.env.CLOCKY_PERMISSION_MODE ?? 'workspace-write'
        workspaceRoot: !!js process.cwd()

    - id: bash-sandbox
      name: '@clocky/clocky-bash-sandbox'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 60000

    - id: pwsh-sandbox
      name: '@clocky/clocky-pwsh-sandbox'
      disabled: !!js process.platform !== 'win32'

    - id: approval
      name: '@clocky/clocky-user-approval'
      config:
        policy: !!js "(process.env.CLOCKY_PERMISSION_MODE ?? 'workspace-write') === 'danger-full-access' ? 'never' : 'ask'"

    - id: permission
      name: '@clocky/clocky-permission-presets'
      config:
        presets:
          read-only:
            sandbox: read-only
            approval: ask
          workspace-write:
            sandbox: workspace-write
            approval: ask
          danger-full-access:
            sandbox: danger-full-access
            approval: never

    - id: shell-env
      name: '@clocky/clocky-shell-env'

    - id: tool-bash
      name: '@clocky/clocky-tool-bash'
      disabled: !!js process.platform === 'win32'

    - id: tool-pwsh
      name: '@clocky/clocky-tool-pwsh'
      disabled: !!js process.platform !== 'win32'

    - id: tool-jobs
      name: '@clocky/clocky-tool-jobs'

    - id: fs-observation-policy
      name: '@clocky/clocky-fs-observation-policy'

    - id: tool-fs
      name: '@clocky/clocky-tool-fs'

    - id: tool-fs-search
      name: '@clocky/clocky-tool-fs-search'
      config:
        sampleOverCapGlobResults: false

    - id: agent-instructions
      name: '@clocky/clocky-agent-instructions'
      config:
        maxBytes: 65536

    - id: skill
      name: '@clocky/clocky-skill'

    - id: skill-filesystem
      name: '@clocky/clocky-skill-filesystem'

    - id: tool-skill
      name: '@clocky/clocky-tool-skill'

    - id: commands
      name: '@clocky/clocky-commands'

    - id: command-feedback
      name: '@clocky/clocky-command-feedback'

    - id: goal
      name: '@clocky/clocky-goal'

    - id: goal-round-driver
      name: '@clocky/clocky-goal-round-driver'

    - id: command-goal
      name: '@clocky/clocky-command-goal'

    - id: plan-mode
      name: '@clocky/clocky-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed only to keep the request shape stable. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

    - id: token-meter
      name: '@clocky/clocky-token-meter'

    - id: compaction-basic
      name: '@clocky/clocky-compaction-basic'

    # Human `/compact`: one useful reduction below the automatic threshold. Backend
    # independent, so it follows whichever compaction service this leaf mounts.
    - id: command-compact
      name: '@clocky/clocky-command-compact'

    - id: subagent
      name: '@clocky/clocky-subagent'

    - id: subagent-spawn-in-process
      name: '@clocky/clocky-subagent-spawn-in-process'
      config:
        providerName: spawn

    - id: subagent-fork-in-process
      name: '@clocky/clocky-subagent-fork-in-process'
      config:
        providerName: fork

    # Continuable background children are selected per delegation tool. The
    # separately loaded follow-up tool registers the one global `send_message`.
    - id: tool-subagent-control
      name: '@clocky/clocky-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@clocky/clocky-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@clocky/clocky-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable

    # Fork stays one-shot: a continuable child's `report` tool and prompt
    # section precede the inherited history a fork exists to reuse; one-shot
    # fork children install neither, keeping the parent's request prefix.
    # See .agents/notes/implemented/architecture/2026-08-10-fork-children-stay-one-shot.md.
    - id: tool-subagent-fork
      name: '@clocky/clocky-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: one-shot

    # Optional direct-child return channel; absent from roots and one-shot agents.
    - id: tool-subagent-report
      name: '@clocky/clocky-tool-subagent-report'

    - id: workflow-worker-thread
      name: '@clocky/clocky-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@clocky/clocky-tool-workflow'

    - id: timeout-policy
      name: '@clocky/clocky-tool-call-timeout-policy'

    - id: spill-local
      name: '@clocky/clocky-spill-local'

    - id: spill-policy
      name: '@clocky/clocky-spill-policy'
      config:
        maxInlineBytes: 50000

    # Durability checkpoints before each model request and top-level dispatch.
    - id: session-checkpoint-policy
      name: '@clocky/clocky-session-checkpoint-policy'

    # Compacts oversized tool results before the broader conversation compactor
    # runs, preserving the model-visible result within the configured budget.
    - id: tool-result-pruner
      name: '@clocky/clocky-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

    - id: tool-todo
      name: '@clocky/clocky-tool-todo'
      config:
        allowParallelInProgress: true

    # Persisted same-session goals reach the model and the slash menu here; the
    # domain, driver, and `/goal` command are above.
    - id: tool-goal
      name: '@clocky/clocky-tool-goal'

    # Fresh-agent Ralph iteration over a build-time-fixed script.
    - id: tool-ralph
      name: '@clocky/clocky-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

    - id: tool-str-replace-editor
      name: '@clocky/clocky-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

    # Consecutive-repeat reminders on the tool chain.
    - id: repeat-tool-reminder
      name: '@clocky/clocky-repeat-tool-reminder'
      config:
        thresholds: [3, 5, 8]
        argumentsPreviewChars: 500

    # Web access is opt-in. The capability and tool remain available to a
    # deployment that mounts a provider, but the base profile has no outbound
    # search or fetch provider.
    - id: web
      name: '@clocky/clocky-web'

    - id: tool-web
      name: '@clocky/clocky-tool-web'
      config:
        fetch: false

    # ── rows every mode mounts, whose values each overlay may state ──────────────

    # The tool registry. Presentation mode is a deployment choice; omitting it here
    # keeps the schema default (native).
    - id: tools
      name: '@clocky/clocky-tools'

    # The deployment persona is a deployment choice; plan-mode and tool plugins own
    # their own prompt sections.
    - id: system-prompt
      name: '@clocky/clocky-system-prompt'
      config:
        persona: ''

    # Agents created at startup. The base stays empty; raw overlays may create
    # agents, while Web creates sessions on client request.
    - id: agent-loop
      name: '@clocky/clocky-agent-loop'
      config:
        agents: []

    # The sandboxed filesystem provider. `cwd` defaults to `process.cwd()`; an
    # overlay can pin another workspace.
    - id: fs-sandbox
      name: '@clocky/clocky-fs-sandbox'
