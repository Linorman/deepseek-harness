/**
 * @clocky/clocky-base — the shared clocky core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `clocky.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @clocky/clocky-base
 */

export {}
