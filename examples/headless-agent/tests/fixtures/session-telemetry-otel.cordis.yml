# Test-only composition: session-telemetry-otel through the real Loader/app
# path, exporting to the mock OTLP collector the driver starts (url via env).
# The redact-rule entry models a deployment mounting its own scrub rule on the
# session-telemetry/record waterfall — the seam itself ships no rules.
- id: logger-console
  name: '@clocky/cordis-plugin-logger-console'
  config:
    colors: false
    levels:
      default: 3
    showTime: ''

- id: cli-mock-llm
  name: './cli-mock-llm.ts'

- id: telemetry-redact-rule
  name: './telemetry-redact-rule.ts'

# Managed child-process groups required by the bash executor.
- id: subprocess
  name: '@clocky/clocky-subprocess-local'

- id: bash
  name: '@clocky/clocky-bash-local'

- id: session-telemetry-otel
  name: '@clocky/clocky-session-telemetry-otel'
  config:
    mode: !!js process.env.CLOCKY_TELEMETRY_E2E_MODE || 'FULL'
    exporter:
      url: !!js process.env.CLOCKY_TELEMETRY_E2E_URL

- id: agent-spine
  name: '@clocky/clocky-agent-spine-demo'
  config:
    agents:
      - id: main
        provider: cli-mock
        model: cli-mock
        cwd: !!js process.cwd()
    persona: 'Test the session-telemetry-otel plugin.'
    workspaceContext: false

- id: persistence
  name: '@clocky/clocky-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: 'none'

- id: checkpoint-policy
  name: '@clocky/clocky-session-checkpoint-policy'
