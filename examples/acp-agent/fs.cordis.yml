# Filesystem-scenario overlay: the sandboxed filesystem stack already lives in
# the base cordis.yml, so this overlay adds only the local tool-result spill
# storage those scenarios exercise.
- id: base
  name: '@clocky/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: spill-local
            name: '@clocky/clocky-spill-local'
            config:
              root: !!js process.env.CLOCKY_SNAPSHOT_SPILL_ROOT ?? './.spill'
          - id: spill-policy
            name: '@clocky/clocky-spill-policy'
            config:
              maxInlineBytes: !!js process.env.CLOCKY_SNAPSHOT && 800 || 50000
